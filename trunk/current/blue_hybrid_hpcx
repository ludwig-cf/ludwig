#@ shell = /bin/ksh
#@ job_type = parallel
#@ node_usage = not_shared
#@ account_no = e19-oh
#@ output = zrun_$(jobid).o$(schedd_host)
#@ error = zrun_$(jobid).e$(schedd_host)
#@ wall_clock_limit = 03:00:00
#@ cpus = 128 
#@ requirements = ( Feature == "SMT" )
#@ queue
pwd

export MP_SHARED_MEMORY=yes
export MEMORY_AFFINITY=MCM
export MP_USE_BULK_XFER=yes
export MP_SINGLE_THREAD=yes

poe ./HLBLC08P
